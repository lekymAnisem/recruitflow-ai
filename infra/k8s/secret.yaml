# WARNING: This is an EXAMPLE template only.
# Do NOT commit real secrets to version control.
# Use your CI/CD pipeline (e.g., Jenkins) or External Secrets Operator
# to inject real values at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: recruitflow-secrets
type: Opaque
stringData:
  jwt-secret: change-me-in-production
  openai-api-key: your-openai-key
  # MONGO_URI: mongodb+srv://user:pass@cluster/recruitflow
