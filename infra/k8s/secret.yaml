# Secrets are managed by Jenkins pipeline using Jenkins credentials.
# See Jenkinsfile — Deploy to EKS stage for the kubectl create secret command.
# Do NOT apply this file. It exists only as a reference.
