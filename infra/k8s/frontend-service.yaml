apiVersion: v1
kind: Service
metadata:
  name: recruitflow-frontend
  namespace: recruitflow
spec:
  type: LoadBalancer
  selector:
    app: recruitflow-frontend
  ports:
    - port: 80
      targetPort: 80
