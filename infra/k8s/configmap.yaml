apiVersion: v1
kind: ConfigMap
metadata:
  name: recruitflow-config
data:
  NODE_ENV: production
  PORT: "5000"
  CORS_ORIGIN: https://recruitflow.example.com
  # MONGO_URI is set via Secret or external secret store in production
